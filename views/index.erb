<section id="add">  
  <form action="/notes" method="post">  
    <textarea name="content" placeholder="Write a note..."></textarea>  
    <input type="submit" value="Take Note!">  
  </form>  
</section>  

<% @notes.each do |note| %>  
  <article <%= 'class="complete"' if note.complete %>>  
    <p>  
      <%= note.content %>  
      <span><a href="/<%= note.id %>">[edit]</a></span>  
    </p>  
    <p class="links">  
      <a href="/<%= note.id %>/complete">&#8623;</a>  
    </p>  
    <p class="meta">Created: <%= note.created_at %></p>  
  </article>  
<% end %>  
 
<!-- action sends the form data to that file(url) -->
<!-- method attribute specifies how to send form data -->
<!-- GET: 
1) appends form data in the format of a key-value pair
2) will show the data in the url
2b) best for pure query data (searches)
3) URL variables

POST:
1) appends form data inside the body of the HTTP request
2) HTTP Post transaction
 -->